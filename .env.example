# API Configuration
# Backend runs on port 4000 by default (see anchor/.env PORT).
# For a physical device / iOS simulator, use your machine's LAN IP, e.g. http://192.168.x.x:4000
EXPO_PUBLIC_API_URL=http://localhost:4000

# Optional: Analytics
EXPO_PUBLIC_ANALYTICS_ENABLED=false
